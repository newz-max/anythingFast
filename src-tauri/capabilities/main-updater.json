{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-updater",
  "description": "Updater and relaunch permissions for the main window only",
  "windows": ["main"],
  "permissions": ["updater:default", "process:default"]
}
